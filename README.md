git clone + cd agentfleet
Copy .env.example → .env and fill keys
Backend: cd backend && uv sync && uv run main.py
Frontend: cd frontend && npm install && npm run dev
Set up Supabase project and run the schema
